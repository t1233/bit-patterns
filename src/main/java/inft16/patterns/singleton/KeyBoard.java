package inft16.patterns.singleton;

public class KeyBoard {

	// Make this class a Singleton such that only one instance exists
	// 1. Make constructor private
	// 2. Add class instance variable (private static ...)
	// 3. Add class method getInstance() which returns the instance variable from step 2
	
}
